pandas>=2.2.0
requests>=2.31.0
# 1.61.0 is the floor and not a preference: `st.cache_data(refresh_mode=...)`
# was added there, and every cached read in the dashboard passes it, so an
# install of 1.60 satisfies a lower pin and then dies with a TypeError at import
# of app.py rather than degrading. The other newer APIs in use are older than
# that - top navigation is 1.46, `width="stretch"` is 1.49, segmented control
# is 1.40 - so this one parameter sets the bar.
streamlit>=1.61.0
# 6.1 is the floor because Kaleido 1.x is: plotly dropped the `plotly.io.kaleido.scope`
# API that older versions drive, and an environment resolving to 5.x beside the
# Kaleido pinned below draws no chart into the printed board.
plotly>=6.1.0
pyyaml>=6.0.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.9
google-cloud-bigquery>=3.25.0
# The warm board snapshot's shared home when JIRA_DASHBOARD_SNAPSHOT_BUCKET is
# set: every Cloud Run instance reads one GCS object instead of gathering the
# whole board cold. Imported lazily, so a local run without the bucket set
# never loads it.
google-cloud-storage>=2.18.0
db-dtypes>=1.2.0
# Printing the whole board. WeasyPrint lays the recorded page out as a PDF and
# needs Pango/Cairo from the image; Kaleido turns each chart into a picture and
# drives the headless Chromium the image installs (BROWSER_PATH points at it).
# Both are optional at runtime: without WeasyPrint the board is offered as HTML,
# without a renderer a chart leaves a labelled gap.
weasyprint>=62.0
kaleido>=1.0.0
